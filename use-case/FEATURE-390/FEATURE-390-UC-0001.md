# FEATURE-390 规范增加版本号默认递增规则 — 测试用例

> 版本：v0.1
> 日期：2026-08-21
> 分支：FEATURE-390
> 说明：本文件验证 `.rules/PROJECT STANDARDS.md` 规范文件落实版本号默认递增规则：FEATURE 分支→版本号默认第二位（minor）递增，FIX 分支→版本号默认第三位（patch）递增，作为"新建版本计划"的默认值，用户可覆盖，major（第一位）递增仍由用户手动决定。

---

## 阶段一：规则内容

### UC-0001 FEATURE 分支默认递增 minor（第二位）
- **前置**：无
- **操作**：检查 `.rules/PROJECT STANDARDS.md` 的"新建开发任务"第 1 步"新建版本计划"
- **预期**：明确 FEATURE 分支（新功能）→ 版本号默认第二位（minor）递增
- **验证**：人工审阅规范文件，确认该规则存在且表述清晰

### UC-0002 FIX 分支默认递增 patch（第三位）
- **前置**：无
- **操作**：检查 `.rules/PROJECT STANDARDS.md` 的"新建开发任务"第 1 步"新建版本计划"
- **预期**：明确 FIX 分支（Bug 修复）→ 版本号默认第三位（patch）递增
- **验证**：人工审阅规范文件，确认该规则存在且表述清晰

### UC-0003 作为默认值，用户可覆盖
- **前置**：无
- **操作**：检查 `.rules/PROJECT STANDARDS.md` 的"新建开发任务"第 1 步"新建版本计划"
- **预期**：明确该规则为默认值，用户可手动覆盖（保留现有[小/中/大/不新建]选择能力）
- **验证**：人工审阅规范文件，确认该表述存在

### UC-0004 major（第一位）递增仍由用户手动决定
- **前置**：无
- **操作**：检查 `.rules/PROJECT STANDARDS.md` 的"新建开发任务"第 1 步"新建版本计划"
- **预期**：明确 major（第一位）递增仍由用户手动决定（重大变更需人工判断），不纳入默认规则
- **验证**：人工审阅规范文件，确认该表述存在

---

## 阶段二：与现有流程衔接

### UC-0005 与现有"新建版本计划"流程衔接自然
- **前置**：无
- **操作**：通读修改后的"新建开发任务"第 1 步
- **预期**：新增的默认递增规则与现有[小/中/大/不新建]选择流程衔接自然，无重复矛盾
- **验证**：人工审阅规范文件

### UC-0006 与 ROADMAP 版本命名规范一致
- **前置**：无
- **操作**：对照 ROADMAP 版本命名规范（patch=修复、minor=新功能）
- **预期**：FEATURE→minor、FIX→patch 的对应关系与 ROADMAP 语义化版本规范一致
- **验证**：人工对照审阅

---

## 阶段三：整体验证

### UC-0007 编译验证
- **前置**：无
- **操作**：运行 `go build ./... && go vet ./... && go build -o work/co-shell .`
- **预期**：编译通过、vet 无告警、可执行码生成成功
- **验证**：命令执行成功

### UC-0008 构建信息更新
- **前置**：无
- **操作**：检查 main.go 中 build 计数
- **预期**：build 计数 +1（484 → 485），ROADMAP 中 FEATURE-390 任务状态更新并标注 `[BUILD-485]`
- **验证**：人工审阅 main.go 和 ROADMAP.md
