# FEATURE-456-UC-0010 同步阻塞式审查

## 前置条件
- 已编译最新可执行码（`go build -o work/co-shell .`）
- 监督 LLM 已启用

## 操作步骤
1. 让主 LLM 完成一个任务并调用 `attempt_completion`
2. 观察主 LLM 是否**暂停**等待监督 LLM 审查结果（同步阻塞）
3. 在监督 LLM 审查期间，观察主 LLM 是否继续执行其他操作（应暂停）
4. 监督 LLM 返回审查结果后，观察主 LLM 是否根据结果继续（放行→用户确认 / 打回→重跑）

## 预期结果
- 主 LLM 调用 `attempt_completion` 后**同步阻塞**，等待监督 LLM 审查结果
- 审查期间主 LLM 不继续执行其他操作
- 监督 LLM 返回结果后，主 LLM 根据结果继续（放行→用户确认 / 打回→重跑）
- 形成完整的"交付门禁"闭环

## 实际结果
（待填写）

## 结论
- [ ] 通过
- [ ] 失败
