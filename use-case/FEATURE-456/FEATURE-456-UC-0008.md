# FEATURE-456-UC-0008 放行后理由+建议向用户报告并进记忆

## 前置条件
- 已编译最新可执行码（`go build -o work/co-shell .`）
- 监督 LLM 已启用

## 操作步骤
1. 让主 LLM 完成一个**完整**的任务，调用 `attempt_completion`
2. 监督 LLM 判定**放行**（approved=true），给出理由和建议
3. 观察监督 LLM 的理由和建议是否**直接向用户报告**（不进入主 LLM 上下文）
4. 观察监督 LLM 的理由和建议是否作为 **user 消息进入记忆**（永久记忆）
5. 检查记忆库中是否记录了监督 LLM 的审查结论

## 预期结果
- 放行时，监督 LLM 的理由+建议直接向用户报告（不进入主 LLM 上下文）
- 放行时，监督 LLM 的理由+建议作为 user 消息进入永久记忆
- 记忆库中能检索到监督 LLM 的审查结论

## 实际结果
（待填写）

## 结论
- [ ] 通过
- [ ] 失败
