# FIX-483-UC-0002: msgviz 红点高度改为 2px

## 前置条件
- Web UI 已启动（serve 模式），浏览器已打开会话
- 会话中已产生至少一条消息（msgviz 图表有线段和红点）

## 操作步骤
1. 打开浏览器开发者工具（F12）
2. 检查 `.msgviz-dot` 元素的计算样式
3. 查看其 `height` 属性值

## 预期结果
- `.msgviz-dot` 的 `height` 为 `2px`（不再是 1px）
- 红点垂直位置计算（app.js 中 `dotH = 2`）与 2px 高度匹配，红点仍正确居中于线段

## 验收标准
- [ ] `.msgviz-dot` 高度为 2px
- [ ] 红点垂直位置仍正确编码上下文占用率（bottom=空，top=满）
