# FIX-301 file_tools 过时测试修复（read_file/search_files/write_to_file）

| 项目 | 内容 |
|------|------|
| 任务编号 | FIX-301 |
| 任务名称 | 修复 file_tools 3 组单测失败（工具参数契约变更后测试未同步） |
| 类型 | 测试修复（过时测试对齐新参数契约） |
| 版本 | v0.7.0 |
| 关联变更 | FEATURE-265（write_to_file 增加 mode 必填）/ FEATURE-250（read_file start_line/end_line 必填）/ i18n（search_files 文案中文化） |

## 背景
P3 基线 900ba06 起 agent 包 TestReadFileTool / TestSearchFilesTool / TestWriteToFileTool 持续失败，经 git worktree 交叉验证确认为**既有问题（非 FEATURE-304 引入）**。本次按循环模式深入调查根因并修复。

## 根因分析（循环模式：每个失败子用例逐一复现 → 对比工具契约 → 归类）

| 失败用例 | 复现错误 | 根因 | 归属 |
|---------|---------|------|------|
| write_new_file / nested dir / empty content | `mode argument is required` | FEATURE-265 后 write_to_file 的 mode（new/rewrite/append）为必填，测试缺 mode | 过时测试 |
| overwrite_existing_file | `mode argument is required` | 同上（应为 rewrite） | 过时测试 |
| read_entire_file / read_with_start_line | `start_line is required` / `end_line is required` | 工具契约 start_line/end_line 均必填，测试缺参数 | 过时测试 |
| search_with_no_matches | 期望英文 `No matches found` 但输出 i18n 中文 | search_files 无结果文案已 i18n 中文化，断言硬编码英文 | 过时断言 |

## 修复
- write_to_file：正向 4 用例补 `mode`（new/rewrite/append 与语义匹配）
- read_file：正向 2 用例 + 错误用例补 `start_line`/`end_line`；file_not_found 补全参数
- search_files：无结果断言改为断言 regex 值（跨语言稳定）

## 循环验证（UC）

- **UC-0001**: `go test ./agent/ -run TestWriteToFileTool` — 6 子用例全 PASS
- **UC-0002**: `go test ./agent/ -run TestReadFileTool` — 7 子用例全 PASS
- **UC-0003**: `go test ./agent/ -run TestSearchFilesTool` — 6 子用例全 PASS
- **UC-0004**: `go test ./agent/` — 全量 PASS（附：此前 3 组 FAIL，修复后 0 FAIL）
- **UC-0005**: 全项目回归 — `go test ./...` 其余包不受影响
- **UC-0006**: 修复范围 — diff 仅限 agent/file_tools_test.go；无生产代码改动