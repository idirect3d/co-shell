# FEATURE-449-UC-0003 高亮清理策略：正式指令清空、工具调用只清背景保留字体

## 前置条件
- 启动 Web UI（`--serve`）
- 工作区为 co-shell 项目根目录

## 操作步骤
1. 输入指令 A，让 LLM 报告受影响对象为 `agent/run_stream.go`，观察其高亮（背景+字体）
2. 在同一个任务中，LLM 再次调用工具报告受影响对象为 `web/static/app.js`，观察 `agent/run_stream.go` 的高亮
3. 在录入框输入新的正式指令 B（新任务），观察所有受影响对象的高亮

## 预期结果
- 步骤 2：`agent/run_stream.go` 的高亮降级为仅字体（蓝色文字，无背景），`web/static/app.js` 为完整高亮（背景+字体）
- 步骤 3：所有受影响对象的高亮（含字体）被完全清空

## 实际结果
（待填写）

## 结论
- [ ] 通过
- [ ] 失败
