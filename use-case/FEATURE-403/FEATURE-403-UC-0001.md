# FEATURE-403 "+"号按钮基准位置向右上方 x/y 都整体移动 3 像素

## 背景

FEATURE-402 将"+"号按钮嵌入到录入框左边，但用户希望"+"号按钮的基准位置再向右上方 x/y 都整体移动 3 像素。

## 验收标准

1. "+"号按钮的基准位置向右上方 x/y 都整体移动 3 像素。
2. 编译通过：`go build ./... && go vet ./... && go build -o work/co-shell .`

## 测试用例

### UC-001 + 号按钮向右上方移动 3 像素
- 前置：Web UI 已加载
- 步骤：检查"+"号按钮位置
- 期望："+"号按钮基准位置向右上方 x/y 都整体移动 3 像素

### UC-002 编译验证
- 前置：无
- 步骤：运行 `go build ./... && go vet ./... && go build -o work/co-shell .`
- 期望：全部通过，无编译错误、无 vet 告警
