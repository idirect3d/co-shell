# FEATURE-389 优化分支规范（防止 LLM 忘记建分支）— 测试用例

> 版本：v0.1
> 日期：2026-08-21
> 分支：FEATURE-389
> 说明：本文件按阶段组织测试用例，验证 `.rules/PROJECT STANDARDS.md` 规范文件落实方案 1+2+3：①开发流程"开始编码"前和"代码提交及合并"前增加强制分支检查；②明确任何代码修改（含小优化）都必须先建任务编号并创建分支；③把"禁止直接在 main 提交"提升为最高优先级红线。

---

## 阶段一：方案 1 — 强制分支检查

### UC-0001 开发流程"开始编码"前增加强制分支检查
- **前置**：无
- **操作**：检查 `.rules/PROJECT STANDARDS.md` 的"开发流程"章节，第 1 步"开始编码"之前
- **预期**：存在强制分支检查步骤，要求执行 `git branch --show-current` 确认当前在功能分支；若在 main 必须先建分支再编码
- **验证**：人工审阅规范文件，确认该步骤存在且表述清晰

### UC-0002 "代码提交及合并"第 3 步"提交合并代码"前增加强制分支确认
- **前置**：无
- **操作**：检查 `.rules/PROJECT STANDARDS.md` 的"代码提交及合并"章节，第 3 步"提交合并代码"之前
- **预期**：存在强制分支确认步骤，要求先确认当前在功能分支（`git branch --show-current`），若在 main 必须先建分支再提交
- **验证**：人工审阅规范文件，确认该步骤存在且表述清晰

### UC-0003 强制分支检查使用非交互命令
- **前置**：无
- **操作**：检查新增的强制分支检查步骤
- **预期**：使用 `git branch --show-current` 等非交互命令，不触发需要用户响应的 git 命令（符合"git操作不要用需要用户响应的命令"原则）
- **验证**：人工审阅规范文件，确认命令为非交互式

---

## 阶段二：方案 2 — 小优化也必须建分支

### UC-0004 明确任何代码修改（含小优化）都必须先建任务编号并创建分支
- **前置**：无
- **操作**：检查 `.rules/PROJECT STANDARDS.md` 的"新建开发任务"章节
- **预期**：明确表述"任何代码修改（包括标题栏优化、颜色优化、冒号优化等小改动）都必须先建任务编号并创建分支，禁止在 main 上直接提交"
- **验证**：人工审阅规范文件，确认该表述存在

### UC-0005 小优化也纳入任务编号流程
- **前置**：无
- **操作**：检查 `.rules/PROJECT STANDARDS.md` 的"新建开发任务"章节第 2 步"建任务编号"
- **预期**：明确小优化（无独立需求规格的小改动）也必须建任务编号（FEATURE-XXX / FIX-XXX），不能跳过
- **验证**：人工审阅规范文件，确认该表述存在

---

## 阶段三：方案 3 — 禁止 main 提交提升为最高红线

### UC-0006 "禁止直接在 main 提交"提升为最高优先级红线
- **前置**：无
- **操作**：检查 `.rules/PROJECT STANDARDS.md` 的"分支策略"章节
- **预期**：明确"禁止直接在 main 上提交"为最高优先级红线，任何情况下（包括小优化）都不得违反；违反视为严重违规
- **验证**：人工审阅规范文件，确认该表述存在且优先级最高

### UC-0007 红线表述醒目
- **前置**：无
- **操作**：检查 `.rules/PROJECT STANDARDS.md` 中红线表述
- **预期**：使用加粗、强调等醒目格式突出"禁止直接在 main 提交"红线
- **验证**：人工审阅规范文件，确认格式醒目

---

## 阶段四：整体验证

### UC-0008 规范文件可读性
- **前置**：无
- **操作**：通读修改后的 `.rules/PROJECT STANDARDS.md`
- **预期**：新增内容与原有流程衔接自然，无重复矛盾，表述清晰
- **验证**：人工审阅规范文件

### UC-0009 编译验证
- **前置**：无
- **操作**：运行 `go build ./... && go vet ./... && go build -o work/co-shell .`
- **预期**：编译通过、vet 无告警、可执行码生成成功
- **验证**：命令执行成功

### UC-0010 构建信息更新
- **前置**：无
- **操作**：检查 main.go 中 build 计数
- **预期**：build 计数 +1（483 → 484），ROADMAP 中 FEATURE-389 任务状态更新并标注 `[BUILD-484]`
- **验证**：人工审阅 main.go 和 ROADMAP.md
