# FEATURE-413 整理前端控件使用规范

## 背景

Web UI 经过多轮迭代，积累了丰富的 HTML 控件（按钮、图标按钮、模态框、下拉菜单、分段控件、小胶囊开关、虚拟键盘选项、事件块、输入参数子块、文件树、状态栏等）。这些控件分散在 `web/static/index.html`、`app.js`、`style.css` 中，缺乏统一的使用规范，未来新增界面控件时可能样式不一致。

## 验收标准

1. 在 `.rules/` 下新建 `前端控件使用规范.md`。
2. 手册系统整理现有前端控件，覆盖：按钮、图标按钮、键盘按钮、虚拟键盘选项键、小胶囊开关、分段控件、开关/输入/下拉、事件块、输入参数子块、模态框、下拉菜单、悬浮菜单、会话菜单、图片预览、提问区域、交互组件、选项按钮组、任务计划、文件树、状态栏、输入行等。
3. 每个控件说明：用途、HTML 结构、CSS 类名、JS 用法、样式要点、使用注意事项。
4. 手册可作为未来界面控件的使用准则，确保控件以统一的方式和样式被使用。
5. 编译通过：`go build ./... && go vet ./... && go build -o work/co-shell .`

## 测试用例

### UC-001 手册文件存在
- 前置：无
- 步骤：检查 `.rules/前端控件使用规范.md`
- 期望：文件存在

### UC-002 覆盖基础按钮控件
- 前置：手册已生成
- 步骤：检查手册中是否包含 `.btn`（含 primary/danger/ghost/sm 变体）、`.icon-btn`（含 sm 变体）、`.key-btn`、`.opt-key-btn`、`.tool-params-raw`、`.mode-seg`、`.set-toggle`/`.set-input`/`.set-select` 等基础控件
- 期望：每个控件都有用途、HTML 结构、CSS 类名、JS 用法、样式要点、注意事项说明

### UC-003 覆盖容器/布局控件
- 前置：手册已生成
- 步骤：检查手册中是否包含 `.ev` 事件块、`.tool-params` 输入参数子块、`.modal` 模态框、`.menu` 下拉菜单、`.logo-menu` 悬浮菜单、`.session-menu` 会话菜单、`.preview` 图片预览、`.ask-area` 提问区域、`.interaction-*` 交互组件、`.option-buttons` 选项按钮组、`.plan-*` 任务计划、`.tree` 文件树、`.statusbar` 状态栏、`.input-row` 输入行等容器/布局控件
- 期望：每个控件都有完整说明

### UC-004 控件说明完整性
- 前置：手册已生成
- 步骤：抽查 3 个控件（如 `.btn`、`.ev`、`.tool-params-raw`）
- 期望：每个控件都包含用途、HTML 结构、CSS 类名、JS 用法、样式要点、注意事项六个维度

### UC-005 可作为使用准则
- 前置：手册已生成
- 步骤：阅读手册的"使用准则"或"通用约定"部分
- 期望：手册包含统一的命名约定、样式约定、交互约定，可作为未来控件开发准则

### UC-006 编译验证
- 前置：无
- 步骤：运行 `go build ./... && go vet ./... && go build -o work/co-shell .`
- 期望：全部通过，无编译错误、无 vet 告警
