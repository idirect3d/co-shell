# FEATURE-514-UC-0012 移除计数在每次真实用户消息开始时重置

## 前置条件
- `.set context-remove-limit 1`

## 操作步骤
1. 触发一次 LLM 调用失败 → 移除 1 次（计数达到上限）
2. 该轮结束后，用户输入一条新消息重新开始
3. 再次触发 LLM 调用失败：观察是否仍允许移除一次

## 预期结果
- 新用户消息开始后计数归零，可再次移除一次（上限按"每轮用户请求"生效，而非全局累计）
- 单元测试 `TestContextRemoveLimit_ResetPerUserTurn` 覆盖

## 实际结果
（待填写）

## 结论
- [ ] 通过
- [ ] 失败
